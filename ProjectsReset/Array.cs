using System;
using System.Collections.Generic;

namespace ProjectsReset
{
    class Array
    {
        //static int _y;//세로
        //static int _x;//가로
        //static List<char>[,] _map = new List<char>[_y, _x];
        //static char _wall = '|';
        //static char _space = ' ';
        //static public void SetMap(int x, int y)
        //{
        //    _x = x;
        //    _y = y;
        //}
        //public static void DrawMap1()
        //{
        //    Console.Clear();
        //    Player.PlayerPos(29, 20);
        //    //총알 위치
        //    Bullet.BulletPlayerPos[0] = Player.PlayerCorePos[0] - 1;
        //    Bullet.BulletPlayerPos[1] = Player.PlayerCorePos[1];
        //    Bullet.BulletEnemyPos[0] = Enemy.EnemyCorePos[0] - 1;
        //    Bullet.BulletEnemyPos[1] = Enemy.EnemyCorePos[1];
        //
        //    //적이 벽에 닿으면 위치 고정
        //    if (Enemy.EnemyLPos[1] <= _x - _x)//왼벽에 적 왼날개 닿으면
        //    {
        //        Enemy.EnemyLPos[1] = _x + 1;
        //        Enemy.EnemyCorePos[1] = _x + (_x + 1);
        //    }
        //    if (Enemy.EnemyRPos[1] >= _x)//오른벽에 적오른날개 닿으면
        //    {
        //        Enemy.EnemyRPos[1] = _x - 1;
        //        Enemy.EnemyCorePos[1] = _y - (_y - 1);
        //    }
        //
        //
        //    //플레이어가 벽에 닿으면 위치 고정
        //    if (Player.PlayerLeftPos[1] <= _x - _x)//왼벽에 플레이어 왼날개 닿으면
        //    {
        //        Player.PlayerLeftPos[1] = _x + 1;
        //        Player.PlayerCorePos[1] = _x + (_x + 1);
        //        Player.PlayerHeadPos[0] = _x + (_x + 1);
        //    }
        //    if (Player.PlayerRightPos[1] >= _x)//오른벽에 플레이어 오른날개 닿으면
        //    {
        //        Player.PlayerRightPos[1] = _x - 1;
        //        Player.PlayerCorePos[1] = _x - (_x - 1);
        //        Player.PlayerHeadPos[0] = _x - (_x - 1);
        //    }
        //    for (int i = 0; i < _x; i++)
        //    {
        //        for (int j = 0; j < _y; j++)
        //        {
        //            if (j == 0 || j == _y - 1)
        //            {
        //                _map[j, i]=new List<char>();
        //                _map[j, i].Add(_wall);
        //               
        //            }
        //            else
        //            {
        //                _map[j, i]=new List<char>();
        //                _map[j, i].Add(_space);
        //
        //            }
        //        }
        //    }
        //}
    }
}
